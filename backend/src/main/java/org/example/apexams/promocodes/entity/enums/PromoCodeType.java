package org.example.apexams.promocodes.entity.enums;

public enum PromoCodeType {
    GLOBAL,           // Applies to all courses and tariffs
    COURSE_SPECIFIC,  // Applies only to specific courses
    TARIFF_SPECIFIC   // Applies only to specific tariffs
}
